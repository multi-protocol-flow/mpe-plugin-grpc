name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: build-${{ matrix.artifact_os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-22.04, artifact_os: linux-x64, bin: mpe_plugin_grpc }
          - { os: windows-latest, artifact_os: windows-x64, bin: mpe_plugin_grpc.exe }
          - { os: macos-latest, artifact_os: macos-x64, bin: mpe_plugin_grpc }
          - { os: macos-latest, artifact_os: macos-arm64, bin: mpe_plugin_grpc }
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake pkg-config perl zlib1g-dev protobuf-compiler
      - name: Install protoc (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install protobuf
      - name: Install protoc (Windows)
        if: matrix.os == 'windows-latest'
        shell: powershell
        run: choco install protobuf -y --no-progress
      - name: Compute version
        id: version
        shell: bash
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            echo "value=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "value=dev-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
          fi
      - name: Inject version from tag
        if: github.ref_type == 'tag'
        shell: bash
        run: |
          python3 - "${{ steps.version.outputs.value }}" <<'EOF'
          import re, sys
          version = sys.argv[1]
          cargo = open("Cargo.toml", encoding="utf-8").read()
          cargo = re.sub(r'^version = "[^"]*"', f'version = "{version}"', cargo, count=1, flags=re.M)
          open("Cargo.toml", "w", encoding="utf-8").write(cargo)
          manifest = open("plugin.json", encoding="utf-8").read()
          manifest = re.sub(r'"version": "[^"]*"', f'"version": "{version}"', manifest, count=1)
          open("plugin.json", "w", encoding="utf-8").write(manifest)
          EOF
      - name: Build frontend (panel + viewer)
        working-directory: frontend
        run: |
          npm ci
          npm run build
      - name: Build (release)
        run: cargo build --release
      - name: Offline tests (roundtrip + integration)
        run: cargo test
      - name: Package zip + sha256
        shell: bash
        run: |
          VERSION=${{ steps.version.outputs.value }}
          mkdir -p dist/grpc
          cp plugin.json dist/grpc/
          cp target/release/${{ matrix.bin }} dist/grpc/
          python3 - "$VERSION" "${{ matrix.artifact_os }}" "${{ matrix.bin }}" <<'EOF'
          import hashlib, os, sys, zipfile
          version, artifact_os, bin = sys.argv[1], sys.argv[2], sys.argv[3]
          os.chmod(f"dist/grpc/{bin}", 0o755)
          name = f"grpc-{version}-{artifact_os}.zip"
          with zipfile.ZipFile(name, "w", zipfile.ZIP_DEFLATED) as z:
              for f in ("plugin.json", bin):
                  z.write(f"dist/grpc/{f}", f"grpc/{f}")
          digest = hashlib.sha256(open(name, "rb").read()).hexdigest()
          open(f"{name}.sha256", "w").write(f"{digest}  {name}\n")
          EOF
      - uses: actions/upload-artifact@v7
        with:
          name: plugin-${{ matrix.artifact_os }}
          path: |
            grpc-*.zip
            grpc-*.zip.sha256

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true
      - uses: softprops/action-gh-release@v3.0.2
        with:
          files: dist/*
